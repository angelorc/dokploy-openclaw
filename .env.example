# ── AI Provider (at least one required) ───────────────────────────────────────
# OpenClaw auto-detects these from environment variables. Set at least one.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
OPENROUTER_API_KEY=
GEMINI_API_KEY=
XAI_API_KEY=
GROQ_API_KEY=
MISTRAL_API_KEY=
CEREBRAS_API_KEY=
VENICE_API_KEY=
MOONSHOT_API_KEY=
KIMI_API_KEY=
MINIMAX_API_KEY=
ZAI_API_KEY=
AI_GATEWAY_API_KEY=
OPENCODE_API_KEY=
OPENCODE_ZEN_API_KEY=
SYNTHETIC_API_KEY=
COPILOT_GITHUB_TOKEN=
XIAOMI_API_KEY=

# ── Amazon Bedrock (uses AWS credential chain) ───────────────────────────────
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=us-east-1

# ── Ollama (local models, no API key needed) ─────────────────────────────────
# OLLAMA_BASE_URL=http://host.docker.internal:11434

# ── Gateway ──────────────────────────────────────────────────────────────────
# Bearer token for gateway auth. Auto-generated and persisted if not set.
OPENCLAW_GATEWAY_TOKEN=
# OPENCLAW_GATEWAY_PORT=18789
# OPENCLAW_GATEWAY_BIND=lan
# OPENCLAW_CONFIG_PATH=/data/.openclaw/openclaw.json
# OPENCLAW_STATE_DIR=/data/.openclaw
# OPENCLAW_WORKSPACE_DIR=/data/workspace

# ── Extra system packages (optional) ──────────────────────────────────────────
# Space-separated list of apt packages to install at image build time.
# Requires rebuilding the image: docker compose build
# OPENCLAW_DOCKER_APT_PACKAGES=ffmpeg build-essential

# ── Port ─────────────────────────────────────────────────────────────────────
# The gateway listens on this port. When deploying behind a reverse proxy
# (Dokploy/Traefik), configure basic auth in Traefik middleware labels.
PORT=18789

# ── Advanced Configuration ────────────────────────────────────────────────────
# For channels (Telegram, Discord, Slack, WhatsApp), browser settings, model
# selection, hooks, and all other OpenClaw options: mount an openclaw.json config
# file or use the Control UI after first boot.
#
# See openclaw.json.example for a reference config template.
# Local Docker:  docker run -v ./openclaw.json:/data/.openclaw/openclaw.json ...
# Dokploy: create a File Mount and use ../files/openclaw.json:/data/.openclaw/openclaw.json:ro
